import { initializeApp } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-app.js";
import { getAuth, setPersistence, browserLocalPersistence, signInWithPopup, GoogleAuthProvider, onAuthStateChanged, signOut } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-auth.js";
import { getFirestore, collection, addDoc, deleteDoc, doc, query, where, onSnapshot, writeBatch } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-firestore.js";

const firebaseConfig = {
  apiKey: "AIzaSyDnneTFty97_4QmcRFOV58AqDUTmzwqtLo",
  authDomain: "bunkhelper-7ae5b.firebaseapp.com",
  projectId: "bunkhelper-7ae5b",
  storageBucket: "bunkhelper-7ae5b.firebasestorage.app",
  messagingSenderId: "377239213325",
  appId: "1:377239213325:web:7fab96687d9f5585318c44",
  measurementId: "G-7ZSZX0SLEH"
};
const app = initializeApp(firebaseConfig);
const auth = getAuth(app);
const db = getFirestore(app);
const DAYS = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"];
const DAY_ORDER = Object.fromEntries(DAYS.map((day,index)=>[day,index]));
let currentUser=null,timetable=[],attendance=[],unsubscribeTimetable=null,unsubscribeAttendance=null,target=Number(localStorage.getItem("bunkhelper-target")||75),importedRows=[],eventsBound=false;
const $=id=>document.getElementById(id);
const escapeHtml=(value="")=>String(value).replace(/[&<>\"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const localISODate=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`};
const todayName=()=>new Date().toLocaleDateString("en-US",{weekday:"long"});
const displayToday=()=>new Date().toLocaleDateString("en-IN",{weekday:"long",day:"numeric",month:"long",year:"numeric"});
const minutes=time=>{const parts=String(time||"00:00").split(":").map(Number);return(parts[0]||0)*60+(parts[1]||0)};
const sortClasses=items=>[...items].sort((a,b)=>(DAY_ORDER[a.day]??99)-(DAY_ORDER[b.day]??99)||String(a.time||"").localeCompare(String(b.time||""))||String(a.subject||"").localeCompare(String(b.subject||"")));
function showToast(message,type="info"){const host=$("toast-host");if(!host)return;const t=document.createElement("div");t.className=`toast toast-${type}`;t.innerHTML=`<span>${escapeHtml(message)}</span><button type="button">×</button>`;t.querySelector("button").onclick=()=>t.remove();host.appendChild(t);setTimeout(()=>t.remove(),4000)}
function switchView(name){document.querySelectorAll("[data-view]").forEach(s=>s.classList.toggle("hidden",s.dataset.view!==name));document.querySelectorAll("[data-nav]").forEach(b=>b.classList.toggle("active",b.dataset.nav===name));$("page-title").textContent=({dashboard:"Dashboard",timetable:"Timetable",attendance:"Mark Attendance",history:"History",settings:"Settings"})[name]||"Dashboard";window.scrollTo({top:0,behavior:"smooth"})}
function bindGoButtons(){document.querySelectorAll("[data-go]").forEach(b=>b.onclick=()=>switchView(b.dataset.go))}
function findScheduledLecture(date,time){if(!date||!time)return null;const weekday=new Date(`${date}T12:00:00`).toLocaleDateString("en-US",{weekday:"long"}),same=timetable.filter(x=>x.day===weekday),exact=same.find(x=>x.time===time);if(exact)return exact;const targetMinutes=minutes(time);const nearest=[...same].sort((a,b)=>Math.abs(minutes(a.time)-targetMinutes)-Math.abs(minutes(b.time)-targetMinutes))[0];return nearest&&Math.abs(minutes(nearest.time)-targetMinutes)<=25?nearest:null}
function findAttendance(subject,date,time=""){return attendance.find(item=>item.subject?.toLowerCase()===subject?.toLowerCase()&&item.date===date&&(!time||item.time===time))}
function refreshSubjectDropdown(){const select=$("att-subject"),previous=select.value,subjects=[...new Set(timetable.map(x=>x.subject).filter(Boolean))].sort((a,b)=>a.localeCompare(b));select.innerHTML=subjects.length?subjects.map(s=>`<option value="${escapeHtml(s)}">${escapeHtml(s)}</option>`).join(""):'<option value="">No timetable classes</option>';select.disabled=!subjects.length;if(subjects.includes(previous))select.value=previous;$("subject-hint").textContent=subjects.length?"Your subject list comes from the saved timetable. Enter date + time to get the scheduled lecture.":"Add your weekly timetable first."}
function suggestLecture(){const match=findScheduledLecture($("att-date").value,$("att-time").value);if(!match){$("suggestion-box").classList.add("hidden");return}$("att-subject").value=match.subject;$("suggestion-box").innerHTML=`<strong>Scheduled lecture:</strong> ${escapeHtml(match.subject)} · ${escapeHtml(match.day)} ${escapeHtml(match.time)}`;$("suggestion-box").classList.remove("hidden")}
function renderToday(){const classes=sortClasses(timetable.filter(item=>item.day===todayName()));$("today-date").textContent=displayToday();$("today-day-title").textContent=classes.length?`${classes.length} ${classes.length===1?"class":"classes"} scheduled today`:"No classes scheduled today";const host=$("today-classes-list");host.innerHTML=classes.length?"":'<div class="empty-inline">No classes scheduled for today.</div>';classes.forEach(item=>{const record=findAttendance(item.subject,localISODate(),item.time);const state=record?.status==="Present"?"present":record?.status==="Absent"?"absent":"upcoming";host.insertAdjacentHTML("beforeend",`<button type="button" class="schedule-chip ${state}" data-today-subject="${escapeHtml(item.subject)}" data-today-time="${escapeHtml(item.time)}"><span class="chip-time">${escapeHtml(item.time)}</span><span class="chip-subject">${escapeHtml(item.subject)}</span><span class="chip-status">${record?escapeHtml(record.status):"Mark"}</span></button>`)})}
function renderMiniTimetable(){const classes=sortClasses(timetable.filter(item=>item.day===todayName()));$("mini-timetable").innerHTML=classes.length?classes.map(item=>`<div class="mini-row"><span class="mini-time">${escapeHtml(item.time)}</span><span class="mini-subject">${escapeHtml(item.subject)}</span><span class="mini-arrow">→</span></div>`).join(""):'<div class="empty-state compact"><div class="empty-icon">📅</div><h4>No classes today</h4><p>Your saved schedule will appear here.</p><button type="button" class="btn" data-go="timetable">Manage timetable</button></div>';bindGoButtons()}
function renderTimetable(){$("timetable-count").textContent=`${timetable.length} ${timetable.length===1?"class":"classes"}`;$("full-timetable").innerHTML=DAYS.map(day=>{const classes=sortClasses(timetable.filter(item=>item.day===day));return`<section class="day-column ${day===todayName()?"is-today":""}"><div class="day-header"><div><span class="day-name">${day}</span><span class="day-count">${classes.length} ${classes.length===1?"class":"classes"}</span></div>${day===todayName()?'<span class="today-badge">TODAY</span>':""}</div><div class="day-items">${classes.length?classes.map(item=>`<div class="class-card"><div><span class="class-time">${escapeHtml(item.time)}</span><h4>${escapeHtml(item.subject)}</h4></div><button type="button" class="icon-btn" data-delete-tt="${item.id}">×</button></div>`).join(""):'<div class="day-empty">No classes</div>'}</div></section>`}).join("")}
function renderStats(){const effective=attendance.filter(item=>item.status!=="Cancelled"),attended=effective.filter(item=>item.status==="Present").length,missed=effective.filter(item=>item.status==="Absent").length,pct=effective.length?Math.round(attended/effective.length*100):0;$("overall-percentage").textContent=`${pct}%`;$ ("total-classes").textContent=effective.length;$ ("total-attended").textContent=attended;$ ("total-missed").textContent=missed;$ ("target-inline").textContent=`${target}%`;$ ("target-threshold").value=String(target);const subjects={};effective.forEach(item=>{subjects[item.subject]??={present:0,total:0};subjects[item.subject].total++;if(item.status==="Present")subjects[item.subject].present++});const rows=Object.entries(subjects);$("subject-analytics").innerHTML=rows.length?rows.map(([subject,data])=>{const pct2=Math.round(data.present/data.total*100),ratio=target/100;let advice;if(pct2>=target){const bunks=Math.max(0,Math.floor((data.present-ratio*data.total)/ratio));advice=bunks?`You can miss ${bunks} more class${bunks===1?"":"es"} and stay at ${target}%.`:`At ${target}%. Keep attending.`}else{const needed=Math.max(1,Math.ceil((ratio*data.total-data.present)/(1-ratio)));advice=`Attend the next ${needed} class${needed===1?"":"es"} to reach ${target}%.`}return`<div class="subject-stat"><div class="subject-stat-top"><div><h4>${escapeHtml(subject)}</h4><span>${data.present}/${data.total} attended</span></div><strong class="${pct2>=target?"good":"bad"}">${pct2}%</strong></div><div class="progress"><span style="width:${Math.min(100,pct2)}%"></span><i style="left:${target}%"></i></div><p>${escapeHtml(advice)}</p></div>`}).join(""):'<div class="empty-state compact"><div class="empty-icon">📊</div><h3>No analytics yet</h3><p>Mark attendance and subject-wise stats will appear here.</p></div>';$("target-summary").innerHTML=`<div class="target-number">${target}%</div><div><strong>Target</strong><p>Current overall: <b>${pct}%</b></p></div>`}
function renderHistory(){const items=[...attendance].sort((a,b)=>`${b.date} ${b.time||""}`.localeCompare(`${a.date} ${a.time||""}`));$("history-list").innerHTML=items.length?`<div class="history-table-wrap"><table class="history-table"><thead><tr><th>Date</th><th>Subject</th><th>Time</th><th>Status</th><th>Type</th><th></th></tr></thead><tbody>${items.map(item=>`<tr><td>${escapeHtml(item.date)}</td><td><strong>${escapeHtml(item.subject)}</strong></td><td>${escapeHtml(item.time||"—")}</td><td><span class="status-pill ${String(item.status).toLowerCase()}">${escapeHtml(item.status)}</span></td><td>${escapeHtml(item.type||"Regular")}</td><td><button type="button" class="text-btn danger" data-delete-att="${item.id}">Delete</button></td></tr>`).join("")}</tbody></table></div>`:'<div class="empty-state compact"><div class="empty-icon">✓</div><h3>No attendance logged</h3><p>Your attendance records will appear here.</p></div>'}

function parseTimeToken(value){let s=String(value||"").trim().replace(/[Oo]/g,"0");let m=s.match(/^(\d{1,2})\s*[:.]\s*(\d{2})\s*(AM|PM)?$/i);if(!m)m=s.match(/^(\d{1,2})\s*(AM|PM)$/i);if(!m)return null;let h=Number(m[1]),min=Number(m[2]||0),ap=(m[3]||"").toUpperCase();if(min>59||h>23)return null;if(ap==="PM"&&h<12)h+=12;if(ap==="AM"&&h===12)h=0;if(!ap&&h<7)h+=12;if(h<6||h>22)return null;return`${String(h).padStart(2,"0")}:${String(min).padStart(2,"0")}`}
function normalizeDayToken(value){const raw=String(value||"").toLowerCase().replace(/[^a-z]/g,"");const map={mon:"Monday",monday:"Monday",tue:"Tuesday",tues:"Tuesday",tuesday:"Tuesday",wed:"Wednesday",wednesday:"Wednesday",thu:"Thursday",thur:"Thursday",thurs:"Thursday",thursday:"Thursday",fri:"Friday",friday:"Friday",sat:"Saturday",saturday:"Saturday"};return map[raw]||null}
function noisySubject(value){const x=String(value||"").replace(/\s+/g," ").trim().toLowerCase().replace(/[^a-z ]/g,"");return!x||x.length<3||/^(break|lunch|recess|holiday|free|off|room|faculty|subject|time)$/.test(x)}
function extractWords(result){return(result?.words||[]).filter(word=>word.text&&word.bbox).map(word=>({...word,text:String(word.text).trim()}))}

function parseTimetableGrid(words){
  const ws=extractWords({words});
  // Find the six day row centers. Saturday may appear as "2nd/4th/5th Saturday".
  const dayAnchors=[];
  for(const w of ws){const t=w.text;const day=normalizeDayToken(t)||(/saturday/i.test(t)?"Saturday":null);if(!day)continue;const y=(w.bbox.y0+w.bbox.y1)/2;if(!dayAnchors.some(x=>x.day===day&&Math.abs(x.y-y)<24))dayAnchors.push({day,y});}
  // Find time headers. They live above the day rows.
  const rawTimes=[];
  for(const w of ws){const time=parseTimeToken(w.text);if(!time)continue;const y=(w.bbox.y0+w.bbox.y1)/2;if(!dayAnchors.length||y<Math.min(...dayAnchors.map(d=>d.y))-10)rawTimes.push({time,x:(w.bbox.x0+w.bbox.x1)/2,y})}
  if(dayAnchors.length<4||rawTimes.length<3)return[];
  const days=dayAnchors.sort((a,b)=>a.y-b.y);
  const times=[];for(const t of rawTimes){if(!times.some(x=>x.time===t.time&&Math.abs(x.x-t.x)<45))times.push(t)}times.sort((a,b)=>a.x-b.x);
  if(times.length<3)return[];
  // Estimate column bands using header centers.
  const centers=times.map(t=>t.x);
  const colBounds=centers.map((c,i)=>({left:i===0?c-(centers[1]-c)/2:(centers[i-1]+c)/2,right:i===centers.length-1?c+(c-centers[i-1])/2:(c+centers[i+1])/2}));
  const out=[];
  for(const d of days){
    const nextDay=days.find(x=>x.y>d.y+1);
    const rowTop=d.y-25,rowBottom=nextDay?Math.min(nextDay.y-12,d.y+52):d.y+52;
    for(let i=0;i<times.length;i++){
      const c=colBounds[i], cellWords=ws.filter(w=>{const x=(w.bbox.x0+w.bbox.x1)/2,y=(w.bbox.y0+w.bbox.y1)/2;if(x<c.left+4||x>c.right-4||y<rowTop||y>rowBottom)return false;if(normalizeDayToken(w.text)||/saturday/i.test(w.text)||parseTimeToken(w.text))return false;return true});
      const subject=cellWords.sort((a,b)=>a.bbox.y0-b.bbox.y0||a.bbox.x0-b.bbox.x0).map(w=>w.text).join(" ").replace(/\s+/g," ").trim();
      if(noisySubject(subject))continue;
      out.push({day:d.day,time:times[i].time,subject,selected:true});
    }
  }
  const seen=new Set();return out.filter(row=>{const k=`${row.day}|${row.time}|${row.subject.toLowerCase()}`;if(seen.has(k))return false;seen.add(k);return true});
}

function parseOCRTextFallback(text){
  // Fallback is deliberately conservative. It can recover simple day + time + subject lines,
  // but it will never manufacture rows when the screenshot structure is ambiguous.
  const lines=String(text||"").split(/\r?\n/).map(line=>line.replace(/\s+/g," ").trim()).filter(Boolean),rows=[];let activeDay=null;
  for(let line of lines){const d=DAYS.find(x=>line.toLowerCase().includes(x.toLowerCase()))||(/saturday/i.test(line)?"Saturday":null);if(d){activeDay=d;line=line.replace(new RegExp(d,"ig")," ")}if(!activeDay)continue;const tm=line.match(/\b\d{1,2}\s*[:.]\s*\d{2}\s*(?:AM|PM)?\b/i);if(!tm)continue;const time=parseTimeToken(tm[0]);if(!time)continue;const subject=line.replace(tm[0],"").replace(/\|/g," ").replace(/\s+/g," ").trim();if(noisySubject(subject))continue;rows.push({day:activeDay,time,subject,selected:true})}
  const seen=new Set();return rows.filter(row=>{const k=`${row.day}|${row.time}|${row.subject.toLowerCase()}`;if(seen.has(k))return false;seen.add(k);return true});
}

function renderImportReview(){const host=$("import-preview-list");if(!importedRows.length){host.innerHTML='<div class="empty-inline">No timetable cells were confidently detected. Try a clear screenshot or use manual entry.</div>';$ ("save-import-btn").disabled=true;return}$ ("save-import-btn").disabled=false;host.innerHTML=importedRows.map((row,index)=>`<div class="import-row"><input type="checkbox" data-import-check="${index}" ${row.selected?"checked":""}><select data-import-day="${index}">${DAYS.map(day=>`<option value="${day}" ${day===row.day?"selected":""}>${day}</option>`).join("")}</select><input type="time" data-import-time="${index}" value="${escapeHtml(row.time)}"><input type="text" data-import-subject="${index}" value="${escapeHtml(row.subject)}"><button type="button" class="text-btn danger" data-remove-import="${index}">Remove</button></div>`).join("")}
async function saveImportedRows(){const host=$("import-preview-list"),rows=importedRows.map((row,i)=>({selected:host.querySelector(`[data-import-check="${i}"]`)?.checked,day:host.querySelector(`[data-import-day="${i}"]`)?.value,time:host.querySelector(`[data-import-time="${i}"]`)?.value,subject:host.querySelector(`[data-import-subject="${i}"]`)?.value?.trim()})).filter(row=>row.selected&&row.day&&row.time&&row.subject&&!noisySubject(row.subject));if(!rows.length){showToast("Select at least one valid class.","warning");return}try{const batch=writeBatch(db),replace=$("replace-timetable").checked;let seen=new Set(replace?[]:timetable.map(x=>`${x.day}|${x.time}|${x.subject.toLowerCase()}`));if(replace)timetable.forEach(item=>batch.delete(doc(db,"timetables",item.id)));let added=0;rows.forEach(row=>{const key=`${row.day}|${row.time}|${row.subject.toLowerCase()}`;if(seen.has(key))return;const ref=doc(collection(db,"timetables"));batch.set(ref,{uid:currentUser.uid,day:row.day,time:row.time,subject:row.subject});seen.add(key);added++});await batch.commit();importedRows=[];$("replace-timetable").checked=false;renderImportReview();showToast(`${added} timetable ${added===1?"class":"classes"} saved.","success")}catch(error){console.error(error);showToast("Could not save imported timetable. Check Firestore rules.","error")}}
async function deleteRecord(collectionName,id){if(!confirm("Delete this record?"))return;try{await deleteDoc(doc(db,collectionName,id));showToast("Record deleted.","success")}catch(error){console.error(error);showToast("Could not delete record.","error")}}
async function saveAttendance(status){const subject=$("att-subject").value,date=$("att-date").value,time=$("att-time").value,type=$("att-type").value;if(!subject||!date||!time||subject==="No timetable classes"){showToast("Choose a lecture, date and time first.","warning");return}if(findAttendance(subject,date,time)){showToast("That lecture is already recorded for this date and time.","warning");return}await addDoc(collection(db,"attendance"),{uid:currentUser.uid,subject,date,time,status,type,timetableMatch:Boolean(findScheduledLecture(date,time))});showToast(`${status}: ${subject}`,status==="Present"?"success":"warning")}
async function clearTimetable(){if(!timetable.length){showToast("Timetable is already empty.","info");return}if(!confirm(`Delete all ${timetable.length} timetable classes?`))return;try{const batch=writeBatch(db);timetable.forEach(item=>batch.delete(doc(db,"timetables",item.id)));await batch.commit();showToast("Timetable cleared.","success")}catch(error){console.error(error);showToast("Could not clear timetable.","error")}}
async function addSampleTimetable(){const sample=[["Monday","09:00","Constitutional Law I"],["Monday","10:00","Law of Contracts II"],["Tuesday","09:00","Consumer Behaviour"],["Tuesday","10:00","Management Accounting"],["Wednesday","09:00","The Bharatiya Nyaya Sanhita 2023"],["Wednesday","10:00","Macroeconomics"],["Thursday","09:00","Business Ethics & CSR"],["Thursday","10:00","Environmental Management"],["Friday","09:00","Constitutional Law I"],["Friday","10:00","Law of Contracts II"]];try{const batch=writeBatch(db);sample.forEach(([day,time,subject])=>{const ref=doc(collection(db,"timetables"));batch.set(ref,{uid:currentUser.uid,day,time,subject})});await batch.commit();showToast("Sample timetable added.","info")}catch(error){console.error(error);showToast("Could not add sample timetable.","error")}}
function bindEvents(){if(eventsBound)return;eventsBound=true;document.querySelectorAll("[data-nav]").forEach(button=>button.onclick=()=>switchView(button.dataset.nav));bindGoButtons();$("logout-btn").onclick=()=>signOut(auth);$("week-view-btn").onclick=()=>switchView("timetable");$("history-view-btn").onclick=()=>switchView("history");$("target-threshold").value=String(target);$("target-threshold").onchange=event=>{target=Number(event.target.value);localStorage.setItem("bunkhelper-target",String(target));renderStats()};$("att-date").value=localISODate();$("att-time").value=new Date().toTimeString().slice(0,5);$("att-date").oninput=suggestLecture;$("att-time").oninput=suggestLecture;$("attendance-form").onsubmit=async event=>{event.preventDefault();try{await saveAttendance($("att-status").value);event.target.reset();$("att-date").value=localISODate();$("att-time").value=new Date().toTimeString().slice(0,5);refreshSubjectDropdown();suggestLecture()}catch(error){console.error(error);showToast("Could not save attendance. Check Firestore rules.","error")}};$("quick-present").onclick=()=>saveAttendance("Present").catch(error=>{console.error(error);showToast("Could not save attendance.","error")});$("quick-absent").onclick=()=>saveAttendance("Absent").catch(error=>{console.error(error);showToast("Could not save attendance.","error")});$("timetable-form").onsubmit=async event=>{event.preventDefault();try{await addDoc(collection(db,"timetables"),{uid:currentUser.uid,subject:$("subject-input").value.trim(),day:$("day-input").value,time:$("time-input").value});event.target.reset();showToast("Class added to timetable.","success")}catch(error){console.error(error);showToast("Could not add class. Check Firestore rules.","error")}};$("clear-timetable-btn").onclick=clearTimetable;$("sample-timetable-btn").onclick=addSampleTimetable;$("export-btn").onclick=()=>{const blob=new Blob([JSON.stringify({exportedAt:new Date().toISOString(),target,timetable,attendance},null,2)],{type:"application/json"}),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download="bunkhelper-backup.json";a.click();URL.revokeObjectURL(url);showToast("Backup exported.","success")};$("full-timetable").onclick=event=>{const id=event.target.closest("[data-delete-tt]")?.dataset.deleteTt;if(id)deleteRecord("timetables",id)};$("history-list").onclick=event=>{const id=event.target.closest("[data-delete-att]")?.dataset.deleteAtt;if(id)deleteRecord("attendance",id)};$("today-classes-list").onclick=event=>{const chip=event.target.closest("[data-today-subject]");if(!chip)return;switchView("attendance");$("att-subject").value=chip.dataset.todaySubject;$("att-date").value=localISODate();$("att-time").value=chip.dataset.todayTime;$("att-status").value="Present";suggestLecture()};$("upload-img-btn").onclick=()=>$("ocr-file-input").click();$("ocr-file-input").onchange=async event=>{const file=event.target.files?.[0];if(!file)return;try{$("ocr-status").textContent="Reading timetable grid…";const worker=await Tesseract.createWorker("eng"),result=await worker.recognize(file);await worker.terminate();const text=result.data?.text||"";$("ocr-preview").textContent=text.trim()||"No readable text found.";$("ocr-preview-wrap").classList.remove("hidden");importedRows=parseTimetableGrid(result.data?.words||[]);if(importedRows.length<3)importedRows=parseOCRTextFallback(text);renderImportReview();$("ocr-status").textContent=importedRows.length?`Detected ${importedRows.length} candidate timetable cells. Review and save below.` : "Text was read, but no timetable cells could be confidently mapped.";showToast(importedRows.length?"Timetable detected — review before saving.":"OCR could not map the timetable reliably.",importedRows.length?"success":"warning")}catch(error){console.error(error);$("ocr-status").textContent="OCR failed.";showToast("Could not read timetable screenshot.","error")}event.target.value=""};$("save-import-btn").onclick=saveImportedRows;$("clear-import-btn").onclick=()=>{importedRows=[];renderImportReview();$("ocr-status").textContent="Nothing imported yet.";$("ocr-preview").textContent="No OCR output yet.";$("ocr-preview-wrap").classList.add("hidden")};$("import-preview-list").addEventListener("input",event=>{const indexRaw=event.target.dataset.importCheck??event.target.dataset.importDay??event.target.dataset.importTime??event.target.dataset.importSubject;if(indexRaw===undefined)return;const i=Number(indexRaw);if(event.target.dataset.importCheck!==undefined)importedRows[i].selected=event.target.checked;if(event.target.dataset.importDay!==undefined)importedRows[i].day=event.target.value;if(event.target.dataset.importTime!==undefined)importedRows[i].time=event.target.value;if(event.target.dataset.importSubject!==undefined)importedRows[i].subject=event.target.value;$("save-import-btn").disabled=!importedRows.some(row=>row.selected&&row.subject.trim()&&row.time)});$("import-preview-list").addEventListener("click",event=>{const index=event.target.closest("[data-remove-import]")?.dataset.removeImport;if(index!==undefined){importedRows.splice(Number(index),1);renderImportReview()}})}
function startListeners(){if(unsubscribeTimetable)unsubscribeTimetable();if(unsubscribeAttendance)unsubscribeAttendance();const ttQuery=query(collection(db,"timetables"),where("uid","==",currentUser.uid)),attQuery=query(collection(db,"attendance"),where("uid","==",currentUser.uid));unsubscribeTimetable=onSnapshot(ttQuery,snapshot=>{timetable=sortClasses(snapshot.docs.map(d=>({id:d.id,...d.data()})));renderToday();renderMiniTimetable();renderTimetable();refreshSubjectDropdown();suggestLecture()},error=>{console.error(error);showToast("Timetable could not be loaded. Check Firebase/Firestore rules.","error")});unsubscribeAttendance=onSnapshot(attQuery,snapshot=>{attendance=snapshot.docs.map(d=>({id:d.id,...d.data()}));renderStats();renderHistory();renderToday()},error=>{console.error(error);showToast("Attendance could not be loaded. Check Firebase/Firestore rules.","error")})}
function signedIn(user){currentUser=user;$("auth-screen").classList.add("hidden");$("app-screen").classList.remove("hidden");$("user-name").textContent=user.displayName||user.email||"Student";$("user-avatar").src=user.photoURL||"https://ui-avatars.com/api/?name=Student&background=171c2d&color=fff";bindEvents();startListeners();switchView("dashboard")}
function signedOut(){currentUser=null;timetable=[];attendance=[];importedRows=[];if(unsubscribeTimetable)unsubscribeTimetable();if(unsubscribeAttendance)unsubscribeAttendance();$("auth-screen").classList.remove("hidden");$("app-screen").classList.add("hidden")}
$("login-btn").onclick=async()=>{try{await setPersistence(auth,browserLocalPersistence);await signInWithPopup(auth,new GoogleAuthProvider())}catch(error){console.error(error);showToast(`Login failed: ${error.message}`,"error")}};
onAuthStateChanged(auth,user=>user?signedIn(user):signedOut());
